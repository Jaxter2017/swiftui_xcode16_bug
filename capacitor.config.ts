import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.swiftui_xcode16_bug',
  appName: 'swiftui_xcode16_bug',
  webDir: 'dist'
};

export default config;
